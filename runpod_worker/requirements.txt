# Worker-side only. The studio orchestrator stays torch-free by design —
# these run on the rented GPU, never on the MacBook.
runpod>=1.6.0
torch>=2.4.0
diffusers>=0.31.0
transformers>=4.44.0
accelerate>=0.34.0
safetensors>=0.4.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
ftfy>=6.2.0
hf-transfer>=0.1.6
# Only needed when AKIO_S3_BUCKET is set (large shots -> object storage).
boto3>=1.34.0
